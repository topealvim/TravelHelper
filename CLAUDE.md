# TravelHelper

## TODO / Reminders
- **IMPORTANT**: After deploying to production, go back to Google Cloud Console → Credentials → edit the Google Maps API key and add HTTP referrer restrictions for your production domain (e.g., `yourdomain.com/*`). This prevents unauthorized usage of your API key.
